import React from "react";
import { Bug, CircleHelp, Eye, MapPin, Sprout } from "lucide-react";
import Link from "next/link";
import Accordion from "@/components/Accordion";
import type { EvaluacionPlaga, ResultadoConsultaV2Publica } from "@/types";

const pestNames: Record<string, string> = { trips: "Trips", aranuela: "Arañuela", orugas_defoliadoras: "Orugas defoliadoras", bolillera_spodoptera: "Bolillera / Spodoptera", chinches: "Chinches" };
const stateLabels: Record<EvaluacionPlaga["estado"], string> = { favorabilidad_alta: "Favorabilidad ambiental alta", favorabilidad_moderada: "Favorabilidad ambiental moderada", sin_condiciones_destacadas: "Sin condiciones destacadas", periodo_relevante_monitoreo: "Período relevante para monitoreo", indeterminado: "No fue posible completar la evaluación", no_evaluada: "No evaluada" };

export function visiblePestEvaluations(result?: ResultadoConsultaV2Publica | null) { return result?.plagas?.evaluaciones.filter((evaluation) => evaluation.estado !== "no_evaluada") ?? []; }
export function pestStateLabel(state: EvaluacionPlaga["estado"]) { return stateLabels[state]; }
export function pestName(group: string) { return pestNames[group] ?? group.split("_").map((part) => part.charAt(0).toUpperCase() + part.slice(1)).join(" "); }

export function PestSummaryCard({ result }: { result: ResultadoConsultaV2Publica }) {
  const evaluations = visiblePestEvaluations(result); if (!evaluations.length) return null;
  const summary = evaluations.length === 1 ? pestStateLabel(evaluations[0].estado) : `${evaluations.length} evaluaciones disponibles`;
  return <Link href="/resultado/plagas" className="flex min-h-[76px] items-center gap-3 rounded-xl border bg-white p-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-amber-50 text-amber-700"><Bug className="h-5 w-5" aria-hidden="true"/></span><span className="min-w-0 flex-1"><strong className="text-xs">Plagas</strong><span className="mt-1 block text-xs font-semibold text-amber-700">{summary}</span></span><span aria-hidden="true" className="text-lg text-[#526477]">›</span></Link>;
}

export function PestResults({ result, compact = false }: { result: ResultadoConsultaV2Publica; compact?: boolean }) {
  const evaluations = visiblePestEvaluations(result); if (!evaluations.length) return null;
  return <section aria-labelledby="pest-results-title" className={compact ? "mt-4" : "mt-3"}><div className="mb-3 flex items-center gap-2"><Bug className="h-5 w-5 text-amber-700" aria-hidden="true"/><h2 id="pest-results-title" className="text-sm font-bold">Plagas</h2></div><div className="grid gap-3 lg:grid-cols-2">{evaluations.map((evaluation) => <PestEvaluation key={`${evaluation.regla}-${evaluation.version}-${evaluation.grupo}`} evaluation={evaluation}/>)}</div></section>;
}

function PestEvaluation({ evaluation }: { evaluation: EvaluacionPlaga }) {
  const informational = evaluation.estado === "indeterminado";
  return <article className={`min-w-0 rounded-xl border bg-white p-5 ${informational ? "border-slate-300" : "border-amber-200"}`}><div className="flex min-w-0 items-start gap-3"><span className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-full ${informational ? "bg-slate-100 text-slate-600" : "bg-amber-50 text-amber-700"}`}>{informational ? <CircleHelp className="h-5 w-5" aria-hidden="true"/> : <Bug className="h-5 w-5" aria-hidden="true"/>}</span><div className="min-w-0"><h3 className="font-bold">{pestName(evaluation.grupo)}</h3><p className={`mt-1 text-sm font-bold ${informational ? "text-slate-600" : "text-amber-800"}`}>{pestStateLabel(evaluation.estado)}</p></div></div><div className="mt-4 flex flex-wrap gap-2 text-[11px] text-[#526477]"><span className="inline-flex items-center gap-1 rounded-full bg-slate-50 px-3 py-1"><MapPin className="h-3.5 w-3.5" aria-hidden="true"/>Zona: {evaluation.zona.replaceAll("_", " ")}</span>{evaluation.fenologia?.tipo === "estimada" ? <span className="inline-flex items-center gap-1 rounded-full bg-slate-50 px-3 py-1"><Sprout className="h-3.5 w-3.5" aria-hidden="true"/>Etapa estimada: {evaluation.fenologia.estado}</span> : null}</div>{evaluation.textos.por_que_se_muestra ? <Block title="¿Por qué te mostramos esto?" text={evaluation.textos.por_que_se_muestra}/> : null}{evaluation.textos.que_significa ? <Block title="Qué significa" text={evaluation.textos.que_significa}/> : null}{evaluation.textos.que_observar ? <Block title="Qué observar" text={evaluation.textos.que_observar} icon/> : null}{evaluation.textos.seguimiento ? <Block title="Seguimiento" text={evaluation.textos.seguimiento}/> : null}{evaluation.motivo && informational ? <p className="mt-4 rounded-lg bg-slate-50 p-3 text-xs text-[#526477]">Motivo: {evaluation.motivo.replaceAll("_", " ")}</p> : null}<div className="mt-4"><Accordion id={`pest-evidence-${evaluation.regla}-${evaluation.grupo}`} title="Ver evidencia técnica" summary={`${evaluation.regla} · versión ${evaluation.version}`}><p>{evaluation.textos.evidencia_tecnica}</p><dl className="mt-3 grid gap-2 text-xs sm:grid-cols-2"><Fact label="Tipo de regla" value={evaluation.tipo_regla.replaceAll("_", " ")}/><Fact label="Evidencia climática" value={evaluation.nivel_evidencia_climatica}/><Fact label="Calidad del dato" value={evaluation.calidad_dato}/><Fact label="Prioridad regional" value={evaluation.prioridad_regional.replaceAll("_", " ")}/></dl>{Object.keys(evaluation.indicadores).length ? <ul className="mt-3 space-y-1 text-xs">{Object.entries(evaluation.indicadores).map(([key, value]) => <li key={key} className="break-words">{key.replaceAll("_", " ")}: <strong>{value}</strong></li>)}</ul> : null}</Accordion></div></article>;
}
function Block({ title, text, icon = false }: { title: string; text: string; icon?: boolean }) { return <section className="mt-4"><h4 className="flex items-center gap-1.5 text-xs font-bold">{icon ? <Eye className="h-4 w-4 text-[#087b4b]" aria-hidden="true"/> : null}{title}</h4><p className="mt-1 break-words text-xs leading-5 text-[#405369]">{text}</p></section>; }
function Fact({ label, value }: { label: string; value: string }) { return <div><dt className="text-[#526477]">{label}</dt><dd className="font-semibold capitalize">{value}</dd></div>; }
